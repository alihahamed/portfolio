"use client";

import React from "react";

export default function Footer() {
  return (
    <footer 
      className="sticky bottom-0 left-0 w-full h-screen bg-[#AB1509] z-0 flex items-center justify-center pointer-events-auto"
      style={{ willChange: "transform" }}
    >
      {/* We will add content here later */}
    </footer>
  );
}
